<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://aswnrj.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://aswnrj.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-05-04T17:57:48+00:00</updated><id>https://aswnrj.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal site of Aswin Raj Rajan — software engineer, building things and writing about them.
</subtitle><entry><title type="html">Why my INT4 and INT8 KV cache quantization gave bitwise-identical perplexity</title><link href="https://aswnrj.github.io/blog/2026/two-pass-perplexity/" rel="alternate" type="text/html" title="Why my INT4 and INT8 KV cache quantization gave bitwise-identical perplexity" /><published>2026-05-04T14:00:00+00:00</published><updated>2026-05-04T14:00:00+00:00</updated><id>https://aswnrj.github.io/blog/2026/two-pass-perplexity</id><content type="html" xml:base="https://aswnrj.github.io/blog/2026/two-pass-perplexity/"><![CDATA[<p>I quantized the KV cache of Llama 3.2-1B to INT4 and to INT8 separately, expecting a small but meaningful perplexity difference between them. Both runs completed. Both reported perplexity <strong>8.591021385729238</strong>. Identical to fifteen decimal places.</p>

<p>That’s not a small numerical difference. That’s the same number. Something was wrong with my benchmark.</p>

<h2 id="the-setup">The setup</h2>

<p>Standard sliding-window perplexity over the <code class="language-plaintext highlighter-rouge">WikiText-2</code> test split. For each window of 2048 tokens, run a forward pass with <code class="language-plaintext highlighter-rouge">past_key_values=fresh_cache</code>, compute cross-entropy on the last 512 positions, accumulate negative log-likelihood, divide by total tokens scored, exponentiate. About 560 windows, ~280K tokens scored.</p>

<p>Three cache configurations, otherwise identical:</p>

<ul>
  <li><strong>FP16</strong>: standard <code class="language-plaintext highlighter-rouge">DynamicCache</code> (no quantization).</li>
  <li><strong>INT8</strong>: HuggingFace <code class="language-plaintext highlighter-rouge">QuantizedCache(backend="hqq", nbits=8, q_group_size=64, residual_length=64)</code>.</li>
  <li><strong>INT4</strong>: HuggingFace <code class="language-plaintext highlighter-rouge">QuantizedCache(backend="hqq", nbits=4, q_group_size=64, residual_length=64)</code>.</li>
</ul>

<p>Expected pattern: <code class="language-plaintext highlighter-rouge">FP16 ≈ INT8 &lt; INT4</code>, with a small gap and a slightly larger one.</p>

<p>What I got:</p>

<table>
  <thead>
    <tr>
      <th>Variant</th>
      <th>Perplexity</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>FP16</td>
      <td>8.59093961953748</td>
    </tr>
    <tr>
      <td>INT8</td>
      <td>8.591021385729238</td>
    </tr>
    <tr>
      <td>INT4</td>
      <td>8.591021385729238</td>
    </tr>
  </tbody>
</table>

<p>INT8 and INT4 are byte-identical. The FP16 vs quantized gap is ~0.0001.</p>

<h2 id="the-diagnosis">The diagnosis</h2>

<p>The realization came from thinking about <em>when</em> the cache is read vs. written during a forward pass.</p>

<p>In an autoregressive decode loop, at step <em>t</em>, the model computes Q for the current token, then attends over <code class="language-plaintext highlighter-rouge">[K_0, ..., K_{t-1}, K_t]</code> where the past <code class="language-plaintext highlighter-rouge">K_i</code> come from the cache. The cache is <strong>read</strong>.</p>

<p>In a single-pass forward over a 2048-token window with an empty starting cache:</p>

<ol>
  <li>Model computes Q, K, V for all 2048 input positions directly from the input tokens.</li>
  <li>Attention runs using these freshly-computed Q, K, V tensors.</li>
  <li>K, V get written to the cache as a side effect.</li>
</ol>

<p>Step 3 is where quantization happens. But step 2, which determines the logits, which determine the perplexity uses the <strong>un-cached, freshly-computed</strong> K and V.</p>

<p><strong>The cache is write-only during a single forward pass.</strong> Quantization affects what’s stored. It does not affect what’s used.</p>

<p>Hence INT8 and INT4 produce identical attention math (both backed by the same un-quantized K, V from the current input), identical logits, and identical perplexity to fifteen decimals.</p>

<h2 id="why-fp16-differs-by-a-tiny-amount">Why FP16 differs by a tiny amount</h2>

<p><code class="language-plaintext highlighter-rouge">DynamicCache</code> (the FP16 path) and <code class="language-plaintext highlighter-rouge">QuantizedCache</code> (INT8/INT4) are different Python classes with different <code class="language-plaintext highlighter-rouge">update()</code> implementations. Even when functionally equivalent for an empty starting cache, they take slightly different code paths (different intermediate tensor allocations, slightly different memory layouts, different order of operations).</p>

<p>The ~0.0001 gap between FP16 and quantized variants has nothing to do with quantization fidelity. It’s just <em>“different class instance -&gt; different floating-point round-off.”</em></p>

<h2 id="the-fix-two-pass-evaluation">The fix: two-pass evaluation</h2>

<p>To exercise the quantized cache, you need a forward pass that <em>reads</em> it. Split each window into two passes:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">prefix</span> <span class="o">=</span> <span class="n">window</span><span class="p">[:,</span> <span class="p">:</span><span class="n">L</span> <span class="o">-</span> <span class="n">score_len</span><span class="p">]</span>   <span class="c1"># 1536 tokens
</span><span class="n">suffix</span> <span class="o">=</span> <span class="n">window</span><span class="p">[:,</span> <span class="n">L</span> <span class="o">-</span> <span class="n">score_len</span><span class="p">:]</span>   <span class="c1"># 512 tokens
</span>
<span class="n">cache</span> <span class="o">=</span> <span class="nf">cache_factory</span><span class="p">()</span>  <span class="c1"># fresh QuantizedCache
</span>
<span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">inference_mode</span><span class="p">():</span>
    <span class="c1"># Pass 1: populate the cache (writes get quantized)
</span>    <span class="n">_</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">prefix</span><span class="p">,</span> <span class="n">past_key_values</span><span class="o">=</span><span class="n">cache</span><span class="p">,</span> <span class="n">use_cache</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">logits_to_keep</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
    <span class="c1"># Pass 2: read from the quantized cache
</span>    <span class="n">output</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">suffix</span><span class="p">,</span> <span class="n">past_key_values</span><span class="o">=</span><span class="n">cache</span><span class="p">,</span> <span class="n">use_cache</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

<span class="c1"># We calculate nll and perplexity from the second pass
</span><span class="n">shift_logits</span> <span class="o">=</span> <span class="n">output</span><span class="p">.</span><span class="n">logits</span><span class="p">[:,</span> <span class="p">:</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="p">:]</span>
<span class="n">shift_labels</span> <span class="o">=</span> <span class="n">suffix</span><span class="p">[:,</span> <span class="mi">1</span><span class="p">:]</span>
<span class="n">loss</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">cross_entropy</span><span class="p">(</span>
    <span class="n">shift_logits</span><span class="p">.</span><span class="nf">float</span><span class="p">().</span><span class="nf">reshape</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">V</span><span class="p">),</span>
    <span class="n">shift_labels</span><span class="p">.</span><span class="nf">reshape</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">),</span>
    <span class="n">reduction</span><span class="o">=</span><span class="sh">'</span><span class="s">sum</span><span class="sh">'</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div>

<p>In Pass 1, the cache fills up. HQQ’s residual buffer flushes every 64 tokens, quantizing those 64 K and V vectors into the main quantized buffer. By the end of Pass 1, the cache holds 1536 tokens in quantized form.</p>

<p>In Pass 2, the suffix’s 512 tokens have Q computed fresh from input. But attention needs K and V over the full sequence, meaning it reads the prefix’s K and V back from the cache. For <code class="language-plaintext highlighter-rouge">QuantizedCache</code>, that read <strong>dequantizes</strong> INT4 or INT8 storage back to FP16 on the fly. The reconstructed values carry quantization noise, and the noise propagates into the suffix’s attention scores and logits.</p>

<p>INT4’s coarser quantization (16 levels vs INT8’s 256) produces larger reconstruction error, which leads to measurably worse perplexity.</p>

<h2 id="the-corrected-numbers">The corrected numbers</h2>

<p>After re-running with two-pass eval:</p>

<table>
  <thead>
    <tr>
      <th>Variant</th>
      <th>Perplexity</th>
      <th>Δ vs FP16</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>FP16</td>
      <td>8.5916</td>
      <td>—</td>
    </tr>
    <tr>
      <td>INT8</td>
      <td>8.5943</td>
      <td>+0.0027 (+0.03%)</td>
    </tr>
    <tr>
      <td>INT4</td>
      <td>9.0431</td>
      <td>+0.4515 (<strong>+5.3%</strong>)</td>
    </tr>
  </tbody>
</table>

<p>INT8 is essentially free: 0.03% perplexity for ~2x cache compression. 
INT4 pays a real cost: 5.3% perplexity for ~3.6x compression. 
<strong>That’s the actual tradeoff</strong> that single-pass eval was hiding.</p>

<p>The numbers broadly agree with literature: KIVI’s 2-bit <em>asymmetric</em> scheme on Llama-2 stays within ~0.1–0.3 perplexity of FP16. My naive symmetric uniform INT4 lands at +0.45, slightly worse than KIVI’s 2-bit because KIVI’s asymmetric K/V handling (per-channel K, per-token V) is specifically designed for cache distributions, where K has outliers and V is well-behaved.</p>

<h2 id="the-general-lesson">The general lesson</h2>

<p>KV cache quantization affects inference <strong>between</strong> forward passes, not <strong>within</strong> a single one. To benchmark its effect on quality, your evaluation has to span at least two forwards that exchange a cache.</p>

<p>Standard sliding-window perplexity, as commonly written in the HuggingFace docs and most tutorials, is a single-pass workload. It’s the right way to evaluate base model quality.</p>

<p>How to fix the methodology:</p>

<p><strong>Two-pass eval per window</strong>: Each window’s cache is populated by a prefix forward, then evaluated by a suffix forward. Fast, simple, costs about one token of evaluation per window.</p>

<p>If you find yourself benchmarking <em>anything</em> that modifies the KV cache like quantization, sparsity, eviction, sliding window, check whether your evaluation actually exercises the cache or just writes to it.</p>

<h2 id="references">References</h2>

<ul>
  <li>Liu, Z., et al. (2024). <em>KIVI: A Tuning-Free Asymmetric 2bit Quantization for KV Cache.</em>.</li>
  <li>Hooper, C., et al. (2024). <em>KVQuant: Towards 10 Million Context Length LLM Inference with KV Cache Quantization.</em>.</li>
  <li>Badri, H., &amp; Shaji, A. <em>HQQ: Half-Quadratic Quantization for Large Language Models.</em>.</li>
  <li>HuggingFace Transformers documentation: <em>Perplexity of fixed-length models.</em></li>
</ul>]]></content><author><name></name></author><category term="machine-learning" /><category term="llm-inference" /><category term="quantization" /><category term="kv-cache" /><category term="benchmarking" /><summary type="html"><![CDATA[When the standard sliding-window perplexity test produced identical numbers to fifteen decimals, the methodology was the bug, not the quantization.]]></summary></entry><entry><title type="html">T4 GPU + Llama: why your attention OOMs at 16K and the one-line fix</title><link href="https://aswnrj.github.io/blog/2026/flashattention-t4/" rel="alternate" type="text/html" title="T4 GPU + Llama: why your attention OOMs at 16K and the one-line fix" /><published>2026-05-03T12:00:00+00:00</published><updated>2026-05-03T12:00:00+00:00</updated><id>https://aswnrj.github.io/blog/2026/flashattention-t4</id><content type="html" xml:base="https://aswnrj.github.io/blog/2026/flashattention-t4/"><![CDATA[<p>I was benchmarking Llama 3.2-1B inference on a free Google Colab T4 GPU, hoping to push the KV cache out to 16K context. The job OOMed at 4K. I needed long-context numbers to be meaningful.</p>

<p>Nothing in the error pointed at anything obviously wrong. The model loaded fine, a small input forward pass was fine, the memory usage looked normal until it didn’t. Somewhere between sequence length 2048 and 8192, GPU memory blew past the 16 GB ceiling.</p>

<p>The cause turned out to be a chain of three independently reasonable things:</p>
<ul>
  <li><strong>T4 is from 2018</strong>, with compute capability 7.5 (Turing). FlashAttention 2 and modern cuDNN attention paths require 8.0+ (Ampere). Hence both are unavailable.</li>
  <li><strong>PyTorch’s</strong> <code class="language-plaintext highlighter-rouge">scaled_dot_product_attenion</code> <strong>(SDPA)</strong> is a dispatcher, not a single kernel. It picks one of four backends at runtime based on hardware, mask shape and a few other features.</li>
  <li>With <strong>T4 + Llama’s GQA</strong>, SDPA falls through to math backend which materializes the full <code class="language-plaintext highlighter-rouge">[B, H, T, T]</code> attention score matrix. At sequence length 16K, that matrix alone is 16GB. Goodbye memory.</li>
</ul>

<p>The fix is a single kwarg in <code class="language-plaintext highlighter-rouge">from_pretrained</code>. This post walks through why the default path falls over, what the fix actually does, and roughly what it costs in compile time and memory overhead. If you’ve ever seen attention OOM on a T4, V100, or any pre-Ampere GPU, this is for you.</p>

<hr />

<h2 id="sdpa-is-a-dispatcher-not-a-kernel">SDPA is a dispatcher, not a kernel</h2>

<div class="row justify-content-center">
  <div class="col-md-4">
    



<figure>
  <picture>
    <!-- Auto scaling with imagemagick -->
    <!--
      See https://www.debugbear.com/blog/responsive-images#w-descriptors-and-the-sizes-attribute and
      https://developer.mozilla.org/en-US/docs/Learn/HTML/Multimedia_and_embedding/Responsive_images for info on defining 'sizes' for responsive images
    -->
    
    <img src="/assets/img/posts/flashattention-t4/blg1.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="Flowchart showing SDPA decision tree" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />
  </picture>

  
</figure>

  </div>
</div>

<p>Pytorch’s <a href="https://docs.pytorch.org/docs/2.11/generated/torch.nn.functional.scaled_dot_product_attention.html"><code class="language-plaintext highlighter-rouge">scaled_dot_product_attention</code></a> (SDPA) looks like a single function:</p>

<p><code class="language-plaintext highlighter-rouge">out = F.scaled_dot_product_attention(Q, K, V, attn_mask=mask, is_causal=True)</code></p>

<p>It’s not. Underneath, it’s a dispatcher that picks one of four implementation backends at call time:</p>
<ol>
  <li><strong>FlashAttention 2</strong>: A tiled, fused implementation. Never materializes the full attention score matrix in HBM, works on-chip in SRAM tiles. Hardware floor: Ampere (SM 8.0+).</li>
  <li><strong>Memory-efficient SDPA</strong>: xformers-derived and similar tiling idea. Broader hardware support than FlashAttention 2 but stricter constraints size of Q, K and V heads.</li>
  <li><strong>cuDNN attention</strong>: NVIDIA’s path. Recent past modes also need Ampere+.</li>
  <li><strong>Math backend</strong>: the textbook formulation. Compute <code class="language-plaintext highlighter-rouge">Q @ K^T</code>, apply mask, softmax, multiply by V. Always available, always correct, materializes the entire <code class="language-plaintext highlighter-rouge">[B, H, T, T]</code> score matrix in memory.</li>
</ol>

<p>The dispatcher tries them in order of most appropriate to least on the basis of various factors. Probably flash 2 first, math last. The first backend whose constraints are satisfied wins. The choice depends on compute capability, tensor dtype, mask shape, whether <code class="language-plaintext highlighter-rouge">is_causal=True</code>, and a handful of other flags.</p>

<p>The most important detail:</p>
<ul>
  <li><strong>The dispatcher is invisible.</strong> You wrote one function call. Whether you got the 4 GB-of-HBM math backend or a tiled fast kernel depends on configuration you can’t see at the API surface. There’s no warning when the dispatcher falls through.</li>
</ul>

<p>If you want to see which backend was selected, PyTorch exposes context managers like <a href="https://docs.pytorch.org/docs/2.11/generated/torch.nn.attention.sdpa_kernel.html"><code class="language-plaintext highlighter-rouge">torch.nn.attention.sdpa_kernel(...)</code></a> to force or restrict backend, useful for debugging fall-through bugs. By default, you get whatever wins.</p>

<hr />

<h2 id="why-each-fast-backend-fails-on-t4--gqa">Why each fast backend fails on T4 + GQA</h2>
<p>Three things have to be true for SDPA to land on a fast tiled kernel: the GPU must support the kernel’s compute primitives, the inputs must satisfy the kernel’s shape constraints, and the dtype must be compatible. T4 + Llama 3.2-1B miss differently per backend.</p>

<div class="row justify-content-center">
  <div class="col-md-6">
    



<figure>
  <picture>
    <!-- Auto scaling with imagemagick -->
    <!--
      See https://www.debugbear.com/blog/responsive-images#w-descriptors-and-the-sizes-attribute and
      https://developer.mozilla.org/en-US/docs/Learn/HTML/Multimedia_and_embedding/Responsive_images for info on defining 'sizes' for responsive images
    -->
    
    <img src="/assets/img/posts/flashattention-t4/blg2.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="Flash Attention vs Math backend" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />
  </picture>

  
</figure>

  </div>
</div>

<h3 id="flashattention-2-hardware-floor">FlashAttention 2: hardware floor</h3>
<p>FlashAttention 2 uses Ampere-specific warp-level matrix instructions. The CUDA kernel is hand-written for these and there’s no graceful degradation to Turing. PyTorch’s dispatcher checks compute_capability &gt;= 8.0, T4 is 7.5, so Flash 2 is skipped immediately.</p>

<h3 id="cudnn-attention-hardware-floor">cuDNN attention: hardware floor</h3>
<p>cuDNN 9.x’s fast attention API also targets Ampere+ tensor cores. Same hardware skip.</p>

<h3 id="memory-efficient-sdpa-the-gqa-trap">Memory-efficient SDPA: the GQA trap</h3>
<p>This is the one that matters. The mem-efficient backend uses block-wise tiled softmax and runs fine on T4 architecturally.</p>

<p>But there’s a constraint I missed: the fused kernels require Q, K, V to have the same number of heads. Llama 3.2-1B uses GQA with 32 Q heads and 8 KV heads (a 4x ratio). transformers passes the tensors at their native shapes: Q is <code class="language-plaintext highlighter-rouge">[1, 32, T, 64]</code>, K and V are <code class="language-plaintext highlighter-rouge">[1, 8, T, 64]</code></p>

<p>The dispatcher catches the rejection and falls through to math.</p>

<p>This is why every modern open LLM hits this on Turing-era GPUs. Llama 3+, Mistral, Qwen 2+, Gemma 2 all use GQA. The fused kernel constraint disqualifies all of them. Pre-Ampere users running any post-2024 open model encounter this on the default SDPA path.</p>

<h3 id="math-whats-left">Math: what’s left</h3>
<p>After Flash 2 and cuDNN are disqualified by hardware and mem-efficient is disqualified by GQA shape, the math backend is what’s left. Math always works. Next section explains what it costs.</p>

<hr />

<h2 id="what-math-backend-costs">What math backend costs</h2>

<p>The math backend is the textbook formulation of attention, computed step by step:</p>

<ol>
  <li><strong>Score matrix</strong>: $S = Q \cdot K^\top$ — shape <code class="language-plaintext highlighter-rouge">[B, H, T, T]</code>.</li>
  <li><strong>Mask</strong>:  fill masked positions with $-\infty$.</li>
  <li><strong>Softmax</strong>: $P = \text{softmax}(S, \text{dim}=-1)$ (same shape as $S$).</li>
  <li><strong>Output</strong>: $O = P \cdot V$ — shape <code class="language-plaintext highlighter-rouge">[B, H, T, d]</code>.</li>
</ol>

<p>Steps 1 and 3 each materialize a full <code class="language-plaintext highlighter-rouge">[B, H, T, T]</code> tensor in HBM. That’s the problem.</p>

<table>
  <thead>
    <tr>
      <th>Sequence length</th>
      <th>Score matrix size (fp16)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>2K</td>
      <td>256 MiB</td>
    </tr>
    <tr>
      <td>4K</td>
      <td>1.0 GiB</td>
    </tr>
    <tr>
      <td>8K</td>
      <td><strong>4.0 GiB</strong></td>
    </tr>
    <tr>
      <td>16K</td>
      <td><strong>16 GiB</strong></td>
    </tr>
    <tr>
      <td>32K</td>
      <td>64 GiB</td>
    </tr>
  </tbody>
</table>

<p>The score tensor <em>alone</em> before the softmax, mask and matmul workspace, exceeds a T4’s 16 GB capacity at sequence length 16384.</p>

<p>At T=8192, my actual run threw the error:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>OutOfMemoryError: CUDA out of memory. Tried to allocate 8.00 GiB.
GPU 0 has a total capacity of 14.56 GiB of which 3.43 GiB is free.
</code></pre></div></div>

<p>The 8 GiB allocation request is on the order of two score-matrix-sized tensors, consistent with the math kernel producing the score matrix and a separate softmax output.</p>

<p>This is what the math backend does by design. It’s correct. It’s also fundamentally $O(T^2)$ in HBM. We cannot support long contexts on a small GPU with this.</p>

<h3 id="the-contrast-tiled-attention">The contrast: tiled attention</h3>

<p>The fast backends solve this by <strong>never materializing the full score matrix.</strong> They tile the computation: for each block of Q, iterate over blocks of K and V, compute the partial scores in on-chip SRAM, apply softmax incrementally using <a href="https://arxiv.org/pdf/1805.02867">online softmax</a>, and accumulate the output block by block.</p>

<p>The full <code class="language-plaintext highlighter-rouge">[B, H, T, T]</code> matrix is never materialized in HBM. Memory stays roughly $O(T)$ instead of $O(T^2)$.</p>

<p>This is what FlashAttention 2, memory-efficient SDPA, and FlexAttention all share. The differences are in <em>which</em> tile sizes, <em>which</em> hardware-specific instructions, and <em>which</em> input shape combinations each kernel handles. We’ve established that the first two don’t work here. The next section is what FlexAttention does differently.</p>

<hr />

<h2 id="enter-flexattention">Enter FlexAttention</h2>

<p><a href="https://docs.pytorch.org/docs/2.11/nn.attention.flex_attention.html">FlexAttention</a> is a PyTorch primitive added in version 2.5 (October 2024). It exposes attention as a Python function you can modify and compile, rather than as a fixed library kernel:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">torch.nn.attention.flex_attention</span> <span class="kn">import</span> <span class="n">flex_attention</span>

<span class="k">def</span> <span class="nf">causal</span><span class="p">(</span><span class="n">score</span><span class="p">,</span> <span class="n">b</span><span class="p">,</span> <span class="n">h</span><span class="p">,</span> <span class="n">q_idx</span><span class="p">,</span> <span class="n">kv_idx</span><span class="p">):</span>
    <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">q_idx</span> <span class="o">&gt;=</span> <span class="n">kv_idx</span><span class="p">,</span> <span class="n">score</span><span class="p">,</span> <span class="o">-</span><span class="nf">float</span><span class="p">(</span><span class="sh">"</span><span class="s">inf</span><span class="sh">"</span><span class="p">))</span>

<span class="n">out</span> <span class="o">=</span> <span class="nf">flex_attention</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">score_mod</span><span class="o">=</span><span class="n">causal</span><span class="p">)</span>
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">score_mod</code> function describes how to modify each entry of the score matrix. You write whatever attention pattern you need. Causal, sliding window, ALiBi, document boundary, anything as Python code. <code class="language-plaintext highlighter-rouge">flex_attention</code> then uses <strong><code class="language-plaintext highlighter-rouge">torch.compile</code> to JIT a Triton kernel</strong> that fuses your modification into a tiled attention computation.</p>

<p>Three properties matter for our problem:</p>

<ol>
  <li><strong>It generates a tiled kernel.</strong> The compiled Triton code computes attention block-by-block in on-chip SRAM, just like FlashAttention 2. It never materializes <code class="language-plaintext highlighter-rouge">[B, H, T, T]</code> in HBM. Memory stays $O(T)$ instead of $O(T^2)$.</li>
  <li><strong>It works on Turing (SM 7.5).</strong> Triton supports T4, V100, and other pre-Ampere architectures. The generated kernel doesn’t depend on Ampere-specific tensor core instructions.</li>
  <li><strong>It handles arbitrary num_heads ratios.</strong> GQA’s 32-Q-head / 8-KV-head mismatch — the constraint that disqualifies the memory-efficient SDPA is just a broadcast pattern in the Triton IR. K and V are virtually expanded inside the kernel.</li>
</ol>

<p>The architectural insight is that <strong>FlexAttention bypasses the SDPA dispatcher entirely.</strong> When <code class="language-plaintext highlighter-rouge">transformers</code> is configured with <code class="language-plaintext highlighter-rouge">attn_implementation="flex_attention"</code>, it swaps its SDPA-based attention layer for one that calls <code class="language-plaintext highlighter-rouge">flex_attention</code> directly with the appropriate <code class="language-plaintext highlighter-rouge">score_mod</code> for the model. None of the Flash 2 / cuDNN / mem-efficient / math fall-through chain runs at all, which means there’s no decision tree to lose to.</p>

<p>That’s the whole story: a kernel compiled for your specific GPU, handling GQA natively, never materializing the $T \times T$ score matrix. The next section is the actual code fix.</p>

<hr />

<h2 id="the-fix-in-code">The fix in code</h2>

<p>Pass <code class="language-plaintext highlighter-rouge">attn_implementation="flex_attention"</code> to <code class="language-plaintext highlighter-rouge">from_pretrained</code>:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">model</span> <span class="o">=</span> <span class="n">AutoModelForCausalLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
    <span class="sh">"</span><span class="s">meta-llama/Llama-3.2-1B</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">torch_dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">,</span>
    <span class="n">attn_implementation</span><span class="o">=</span><span class="sh">"</span><span class="s">flex_attention</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># this line
</span><span class="p">).</span><span class="nf">cuda</span><span class="p">()</span>
</code></pre></div></div>

<p>That’s it. <code class="language-plaintext highlighter-rouge">transformers</code> swaps its SDPA-based attention layer for one that calls <code class="language-plaintext highlighter-rouge">flex_attention</code> with a causal <code class="language-plaintext highlighter-rouge">score_mod</code> baked in for Llama. Forward passes go through Triton instead of the SDPA dispatcher.</p>

<p>Empirical effect on the same workload that OOMed before:</p>

<table>
  <thead>
    <tr>
      <th>Configuration</th>
      <th>Peak GPU memory at T=8192</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">attn_implementation="sdpa"</code> (default)</td>
      <td>8 GiB allocation request fails</td>
      <td>OOM</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">attn_implementation="flex_attention"</code></td>
      <td>3.06 GiB total (weights + cache + workspace)</td>
      <td>runs cleanly</td>
    </tr>
  </tbody>
</table>

<p>The forward pass works on the same input the math backend couldn’t fit. No other code changes. The rest of the model, KV cache wiring, and generation logic are identical. The attention kernel underneath is different, everything above it is the same.</p>

<h3 id="requirements">Requirements</h3>

<ul>
  <li><strong>PyTorch ≥ 2.5</strong> for FlexAttention itself.</li>
  <li><strong>transformers</strong> with the FlexAttention integration (recent versions are fine).</li>
  <li><strong>Triton</strong> (auto-installed alongside modern PyTorch).</li>
</ul>

<hr />

<h2 id="what-flexattention-costs">What FlexAttention costs</h2>

<p>The dominant cost is <strong>compile time</strong>.</p>

<h3 id="compile-time">Compile time</h3>

<p>The first forward pass with a given input shape triggers <code class="language-plaintext highlighter-rouge">torch.compile</code> to JIT a Triton kernel which typically takes 1-30 seconds depending on shape and Triton’s compile cache state. Subsequent forwards at the <strong>same shape</strong> reuse the cached kernel and run at full speed.</p>

<p>Different shapes recompile. A sweep across <code class="language-plaintext highlighter-rouge">[512, 1K, 2K, 4K, 8K, 16K]</code> sequence lengths triggers six compilations over the run. Standard mitigation: warm up before measuring. In my benchmark, <code class="language-plaintext highlighter-rouge">n_warmup=3</code> per sequence length absorbs compile time before the timed iterations start.</p>

<p>If you skip warmup, your first measurement is dominated by compile and looks roughly 10–100x slower than steady state.</p>

<h3 id="memory-overhead">Memory overhead</h3>

<p>FlexAttention’s compile cache and Triton runtime state contribute some memory overhead, but at the scale this benchmark runs at, it’s small enough that I can’t cleanly isolate it from activations. At T=512, peak memory is 2.36 GiB; model weights alone account for 2.30 GiB, leaving roughly 60 MiB for KV cache (16 MiB), prefill activations, and any FlexAttention state combined. The workspace is real but modest.</p>

<p>For the long-context use case FlexAttention is actually built for, any fixed overhead is dwarfed by what the math backend would have allocated for the score matrix.</p>

<h3 id="a-methodology-note">A methodology note</h3>

<p><code class="language-plaintext highlighter-rouge">torch.compile</code>’s cache plus PyTorch’s caching allocator state accumulate across runs. After re-running the sweep multiple times in a single Colab session, peak memory readings drifted upward by hundreds of MiB even with <code class="language-plaintext highlighter-rouge">torch.cuda.empty_cache()</code> between runs. Restarting the Colab kernel between major experiments fixed it. Worth flagging if you’re chasing precise numbers.</p>

<hr />

<h2 id="empirical-numbers">Empirical numbers</h2>

<p>Running the same Llama 3.2-1B forward pass on T4 with <code class="language-plaintext highlighter-rouge">attn_implementation="flex_attention"</code> engaged:</p>

<table>
  <thead>
    <tr>
      <th>Seq length</th>
      <th>Peak memory</th>
      <th>Prefill tok/s</th>
      <th>Decode tok/s</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>512</td>
      <td>2.36 GiB</td>
      <td>4,647</td>
      <td>41.6</td>
    </tr>
    <tr>
      <td>1K</td>
      <td>2.40 GiB</td>
      <td>1,602</td>
      <td>43.1</td>
    </tr>
    <tr>
      <td>2K</td>
      <td>2.50 GiB</td>
      <td>942</td>
      <td>28.7</td>
    </tr>
    <tr>
      <td>4K</td>
      <td>2.69 GiB</td>
      <td>514</td>
      <td>42.6</td>
    </tr>
    <tr>
      <td>8K</td>
      <td>3.06 GiB</td>
      <td>269</td>
      <td>43.2</td>
    </tr>
    <tr>
      <td>16K</td>
      <td>3.82 GiB</td>
      <td>137</td>
      <td>35.9</td>
    </tr>
  </tbody>
</table>

<p>FP16 weights, batch size 1, single T4 (16 GB). Median of 10 timed runs after 3 warmup iterations. Full data and code at <a href="https://github.com/aswnrj/quantization-benchmark">github.com/aswnrj/quantization-benchmark</a>.</p>

<p>Main observation:</p>

<p><strong>Memory stays bounded.</strong> Peak at 16K is 3.82 GiB. Almost 12 GiB of headroom on a 16 GB T4. Extrapolating linearly, FP16 KV cache hits the device ceiling around T=32K. The default SDPA path doesn’t reach 16K at all on this GPU, the math backend OOMs allocating the score matrix.</p>

<p>The headline finding: switching from <code class="language-plaintext highlighter-rouge">attn_implementation="sdpa"</code> (default) to <code class="language-plaintext highlighter-rouge">attn_implementation="flex_attention"</code> is the difference between <strong>running at all</strong> and <strong>OOMing</strong>, on any sequence length above ~4K with this model on this GPU.</p>

<hr />
<h2 id="takeaway">Takeaway</h2>

<p>The interaction is hardware, backend and model architecture. None of the three is unusual on its own; together, they conspire to OOM at moderate sequence length on consumer GPUs.</p>

<p>The lesson generalizes:</p>

<ul>
  <li><strong>If you’re running a modern GQA LLM on any pre-Ampere GPU</strong> like T4 (Turing), V100 (Volta), P100 (Pascal), the default <code class="language-plaintext highlighter-rouge">attn_implementation="sdpa"</code> path will OOM at moderate seq_len. Switch to FlexAttention.</li>
  <li><strong>If you OOM in attention with no obvious cause</strong>, profile with <code class="language-plaintext highlighter-rouge">torch.profiler.profile()</code> and look at kernel names. <code class="language-plaintext highlighter-rouge">attention_math</code> or <code class="language-plaintext highlighter-rouge">bmm</code>-heavy entries mean the SDPA dispatcher fell through to math.</li>
  <li><strong>If you’re benchmarking attention modifications</strong>, don’t assume <code class="language-plaintext highlighter-rouge">F.scaled_dot_product_attention</code> is using the kernel you think. Force a backend explicitly with <code class="language-plaintext highlighter-rouge">torch.nn.attention.sdpa_kernel(...)</code> to verify.</li>
</ul>

<p>The dispatcher is silent about its choices. That silence is the trap.</p>

<p>For the broader picture: PyTorch’s SDPA dispatcher was designed when most production training was on Ampere or newer. Pre-Ampere paths exist but are second-class citizens, particularly for the attention shapes that GQA and MQA architectures emit. FlexAttention sidesteps this by being a universal compile target. Triton runs everywhere PyTorch runs.</p>

<p>If you’re on Colab/Kaggle’s free tier (where T4 is still the default GPU) or working with older datacenter clusters (V100s remain common), this matters in practice. If you’ve migrated to A100/H100, none of this applies, the SDPA fast path is fine.</p>

<hr />
<h2 id="references">References</h2>

<ul>
  <li>Dao, T. (2023). <em>FlashAttention-2: Faster Attention with Better Parallelism and Work Partitioning.</em>.</li>
  <li>Rabe, M. N., &amp; Staats, C. (2021). <em>Self-attention Does Not Need O(n²) Memory.</em>.</li>
  <li>Milakov, M., &amp; Gimelshein, N. (2018). <em>Online normalizer calculation for softmax.</em>.</li>
  <li>Haziza, D., et al. <em>xFormers: A Modular and Hackable Transformer Modelling Library.</em></li>
  <li>Guessous, D., He, H., et al. (2024). <em>FlexAttention: The Flexibility of PyTorch with the Performance of FlashAttention.</em>.</li>
  <li>Tillet, P., Kung, H. T., &amp; Cox, D. (2019). <em>Triton: An Intermediate Language and Compiler for Tiled Neural Network Computations.</em>.</li>
</ul>]]></content><author><name></name></author><category term="machine-learning" /><category term="llm-inference" /><category term="pytorch" /><category term="gpu" /><summary type="html"><![CDATA[A walk through why PyTorch's SDPA falls through to a memory-blowup path on Turing GPUs, and what FlexAttention does differently.]]></summary></entry></feed>